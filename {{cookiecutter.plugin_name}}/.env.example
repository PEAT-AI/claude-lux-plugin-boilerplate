# {{cookiecutter.plugin_display_name}} Configuration
#
# Copy this file to .env and fill in your values.
# Never commit .env to version control!

# Enable verbose logging
# {{cookiecutter.plugin_name | upper}}_VERBOSE=true

# Override data directory (default: ~/.local/share/{{cookiecutter.plugin_name}})
# {{cookiecutter.plugin_name | upper}}_DATA_DIR=/path/to/data

# Override log directory (default: ~/.local/state/{{cookiecutter.plugin_name}}/logs)
# {{cookiecutter.plugin_name | upper}}_LOG_DIR=/path/to/logs
